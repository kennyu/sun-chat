# Local dev, reads .env.local 
# Using convex database - dev:energized-squid-89
# Google Auth and JWT Tokens

# Web app on http://localhost:8081
# Expo app on exp://192.168.1.170:8081

npm run start

# Runs the cloud functions + migrations
npx convex dev 


# Github actions
# Build and push onto Expo using 
# Url: 


# If you need auth, stick with Clerk. Learn it once, implement it on everything
# Convex is great for react projects
# Supabase preferable over firebase

# Agent Workflows

# Codex / the iterative let's discuss mode
# https://steipete.me/posts/just-talk-to-it

# BMADD - Claude CLI and PRD Shards

# Velocity Coding
# Guo's 

# Don't use beta features, unless you know that part of the stack well
# Rely on well-designed abstractions + vendors, don't spend more than an hour struggling

  [+] authAccounts.providerAndAccountId   provider, providerAccountId, _creationTime
  [+] authAccounts.userIdAndProvider   userId, provider, _creationTime
  [+] authRateLimits.identifier   identifier, _creationTime
  [+] authRefreshTokens.sessionId   sessionId, _creationTime
  [+] authRefreshTokens.sessionIdAndParentRefreshTokenId   sessionId, parentRefreshTokenId, _creationTime
  [+] authSessions.userId   userId, _creationTime
  [+] authVerificationCodes.accountId   accountId, _creationTime
  [+] authVerificationCodes.code   code, _creationTime
  [+] authVerifiers.signature   signature, _creationTime
  [+] memberships.by_room   roomId, _creationTime
  [+] memberships.by_user   userId, _creationTime
  [+] messages.by_room_time   roomId, createdAt, _creationTime
  [+] presence.by_room   roomId, _creationTime
  [+] presence.by_user   userId, _creationTime
  [+] receipts.by_msg   messageId, _creationTime
  [+] rooms.by_creator   createdBy, _creationTime